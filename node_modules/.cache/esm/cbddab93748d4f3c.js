let Joi,Prc;_748‍.x([["default",()=>_748‍.o]]);_748‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_748‍.w("../Model/Pcr",[["default",["Prc"],function(v){Prc=v}]]);


const date = require("date-and-time");
const PrcController = {
  async itemsPrc(req, res, next) {
    let item;
    try {
      item = await Prc.find();
      if (!item) {
        return next(new Error("items not found!"));
      }
    } catch (error) {
      return next(error);
    }
    res.json(item);
  },

  async getPrevStockInInfo(req, res) {
    let pre;
    // if (!req.body.from || !req.body.to) {
    //   console.log(req.body.to, "jkbjkb");
    //   res.status(400).send("Bad Request");
    // } else {
      let d1 = date.parse(req.body.to, "YYYY/MM/DD");
      let d2 = date.parse(req.body.from, "YYYY/MM/DD"); //format - '2023/01/10'
      try {
        pre = await Prc.find({
          name: req.body.name,
          requiredTest: req.body.requiredTest,
          sampleType: req.body.sampleType,
          $and: [{ to: { $gt: d1 } }, { from: { $lt: d2 } }],
        });
      } catch (error) {
        return next(error);
      }
// res.json(pre)
      res.status(200).send({ msg: "success", pre });
      _748‍.g.console.log(pre);
    
  },

  async addPrc(req, res, next) {
    const productsSchema = Joi.object({
      name: Joi.string().required(),
      workOder: Joi.string().required(),
      noofSample: Joi.string().required(),
      requiredTest: Joi.string().required(),
      sampleType: Joi.string().required(),
      date: Joi.date().required(),
       lab:Joi.string().required(),
    });
    const { error } = productsSchema.validate(req.body);
    if (error) {
      return next(error);
    }
    const { name, workOder, noofSample, requiredTest, sampleType, date,lab } =
      req.body;

    let product;
    try {
      product = await Prc.create({
        name,
        workOder,
        noofSample,
        requiredTest,
        sampleType,
        date,
       lab,
      });

      if (!product) {
        return next(new Error("Product Not Add"));
      }
    } catch (error) {
      return next(error);
    }
    res.json(product);
  },

  async updatePrc(req, res, next) {
    // const updateSchema = Joi.object({
    //   name: Joi.string(),
    //   workOder: Joi.number(),
    //   noofSample: Joi.number(),
    //   requiredTest: Joi.string(),
    //   sampleType: Joi.string(),
    //   date: Joi.date(),
    // });
    // const { error } = updateSchema.validate(req.body);
    // if (error) {
    //   return next(error);
    // }
    const { name, workOder, noofSample, requiredTest, sampleType, date ,lab} =
      req.body;
 _748‍.g.console.log(req.body,'reqbody')
    let product;
    try {
      product = await Prc.findByIdAndUpdate(
        { _id:req.params.id },
        {
          name,
          workOder,
          noofSample,
          requiredTest,
          sampleType,
          date,
          lab,
        },{new: true}
      );
_748‍.g.console.log(product,'product')
_748‍.g.console.log(req.params.id,'id')
      
    } catch (error) {
      return next(error);
    }
    res.json(product);
  },

  async deletePrc(req, res, next) {
    let product;
    try {
      product = await Prc.findByIdAndRemove({ _id: req.params.id });
      if (!product) {
        return next(new Error("Noting to delete"));
      }
    } catch (error) {
      return next(error);
    }
    res.json(product);
  },
};
_748‍.d(PrcController);
